import { BrowserRouter } from "react-router-dom";
import {Layout} from "@app/layout";

const App = () =>  {

    return (
        <BrowserRouter>
            <Layout />
        </BrowserRouter>
    )
}

export default App
